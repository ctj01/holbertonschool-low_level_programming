#include <stdio.h>
/**
 *main - check the code for Holberton School students.
 *@i : is a parameter
 *@argv : is a parameter
 * Return: nothing.
 */

int main(int i, char *argv[])
{
	i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
