#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int i = 0;
int m = 1; 
while (i < 10 && m < 10)
{
if ( i != m )
{
putchar(i % 10 + '0');
putchar(m % 10 + '0');
if (i < 10)
{
putchar(',');
putchar(' ');
} 
}
i++;
}
putchar('\n');
return (0);
}
