#include "holberton.h"
#include <stdio.h>
#include <string.h>
/**
 *_strlen - check the code for Holberton School students.
 *@s: is a parameter
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int len = strlen(s);

	printf("%i\n", len);
	return (0);

}
