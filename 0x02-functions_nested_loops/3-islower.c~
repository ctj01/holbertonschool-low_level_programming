#include <stdio.h>
#include <ctype.h>
/**
 *main - entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int r;
r = islower('H');
putchar(r + '0');
r = islower('o');
putchar(r + '0');
r = islower(108);
putchar(r + '0');
putchar('\n');
return (0);
}
